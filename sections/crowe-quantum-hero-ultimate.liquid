{{ 'crowe-visual-effects-master.css' | asset_url | stylesheet_tag }}
{{ 'crowe-quantum-hero-ultimate.css' | asset_url | stylesheet_tag }}

{%- style -%}
  #quantum-hero-ultimate-{{ section.id }} {
    position: relative;
    min-height: 100vh;
    overflow: hidden;
    background: 
      radial-gradient(ellipse at 30% 70%, rgba(139, 69, 19, 0.3) 0%, transparent 60%),
      radial-gradient(ellipse at 70% 30%, rgba(85, 107, 47, 0.3) 0%, transparent 60%),
      radial-gradient(ellipse at center, #2F4F4F 0%, #1C2833 50%, #0B0B0B 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
  }

  /* Enhanced Mycelium Network Background Pattern */
  #quantum-hero-ultimate-{{ section.id }}::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 120%;
    height: 120%;
    background: url("data:image/svg+xml,%3Csvg width='120' height='120' viewBox='0 0 120 120' xmlns='http://www.w3.org/2000/svg'%3E%3Cdefs%3E%3ClinearGradient id='myceliumGrad' x1='0%25' y1='0%25' x2='100%25' y2='100%25'%3E%3Cstop offset='0%25' style='stop-color:%23FFD700;stop-opacity:0.6'/%3E%3Cstop offset='50%25' style='stop-color:%23D4AF37;stop-opacity:0.4'/%3E%3Cstop offset='100%25' style='stop-color:%23B8860B;stop-opacity:0.3'/%3E%3C/linearGradient%3E%3C/defs%3E%3Cg stroke='url(%23myceliumGrad)' fill='none' stroke-width='0.8' opacity='0.4'%3E%3Cpath d='M10,10 Q25,5 40,15 Q55,25 70,10 Q85,5 100,20 Q110,35 95,50' stroke-width='1.2'/%3E%3Cpath d='M5,30 Q20,20 35,35 Q50,50 65,30 Q80,15 95,35 Q110,55 100,70' stroke-width='1'/%3E%3Cpath d='M15,50 Q30,40 45,55 Q60,70 75,50 Q90,35 105,55 Q115,70 105,85' stroke-width='1.2'/%3E%3Cpath d='M5,70 Q20,60 35,75 Q50,90 65,70 Q80,55 95,75 Q110,90 100,105' stroke-width='1'/%3E%3Cpath d='M25,90 Q40,80 55,95 Q70,110 85,90 Q100,75 115,95' stroke-width='1.2'/%3E%3Cpath d='M10,10 Q5,25 15,40 Q25,55 10,70 Q5,85 20,100' stroke-width='1'/%3E%3Cpath d='M30,5 Q25,20 35,35 Q45,50 30,65 Q25,80 40,95 Q50,110 45,115' stroke-width='1.2'/%3E%3Cpath d='M50,10 Q45,25 55,40 Q65,55 50,70 Q45,85 60,100' stroke-width='1'/%3E%3Cpath d='M70,5 Q65,20 75,35 Q85,50 70,65 Q65,80 80,95 Q90,110 85,115' stroke-width='1.2'/%3E%3Cpath d='M90,10 Q85,25 95,40 Q105,55 90,70 Q85,85 100,100 Q110,110 105,115' stroke-width='1'/%3E%3C/g%3E%3Cg fill='%23FFD700' opacity='0.6'%3E%3Ccircle cx='15' cy='15' r='2.5' /%3E%3Ccircle cx='40' cy='20' r='2' /%3E%3Ccircle cx='65' cy='15' r='2.5' /%3E%3Ccircle cx='90' cy='25' r='2' /%3E%3Ccircle cx='20' cy='40' r='2' /%3E%3Ccircle cx='45' cy='45' r='2.5' /%3E%3Ccircle cx='70' cy='40' r='2' /%3E%3Ccircle cx='95' cy='50' r='2.5' /%3E%3Ccircle cx='25' cy='65' r='2.5' /%3E%3Ccircle cx='50' cy='70' r='2' /%3E%3Ccircle cx='75' cy='65' r='2.5' /%3E%3Ccircle cx='100' cy='75' r='2' /%3E%3Ccircle cx='15' cy='90' r='2' /%3E%3Ccircle cx='40' cy='95' r='2.5' /%3E%3Ccircle cx='65' cy='90' r='2' /%3E%3Ccircle cx='90' cy='100' r='2.5' /%3E%3C/g%3E%3Cg fill='%23D4AF37' opacity='0.4'%3E%3Ccircle cx='30' cy='10' r='1.5' /%3E%3Ccircle cx='55' cy='25' r='1' /%3E%3Ccircle cx='80' cy='10' r='1.5' /%3E%3Ccircle cx='105' cy='30' r='1' /%3E%3Ccircle cx='10' cy='55' r='1' /%3E%3Ccircle cx='35' cy='60' r='1.5' /%3E%3Ccircle cx='60' cy='55' r='1' /%3E%3Ccircle cx='85' cy='65' r='1.5' /%3E%3Ccircle cx='30' cy='80' r='1.5' /%3E%3Ccircle cx='55' cy='85' r='1' /%3E%3Ccircle cx='80' cy='80' r='1.5' /%3E%3Ccircle cx='105' cy='90' r='1' /%3E%3C/g%3E%3Cg fill='%23B8860B' opacity='0.3'%3E%3Ccircle cx='5' cy='20' r='1' /%3E%3Ccircle cx='25' cy='35' r='0.8' /%3E%3Ccircle cx='45' cy='30' r='1' /%3E%3Ccircle cx='65' cy='45' r='0.8' /%3E%3Ccircle cx='85' cy='30' r='1' /%3E%3Ccircle cx='105' cy='45' r='0.8' /%3E%3Ccircle cx='15' cy='75' r='0.8' /%3E%3Ccircle cx='35' cy='90' r='1' /%3E%3Ccircle cx='55' cy='75' r='0.8' /%3E%3Ccircle cx='75' cy='90' r='1' /%3E%3Ccircle cx='95' cy='85' r='0.8' /%3E%3Ccircle cx='115' cy='100' r='1' /%3E%3C/g%3E%3C/svg%3E");
    animation: mycelium-drift 40s linear infinite;
    z-index: 1;
  }

  /* Enhanced floating spores and mycelial particles overlay */
  #quantum-hero-ultimate-{{ section.id }}::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 110%;
    height: 110%;
    background: url("data:image/svg+xml,%3Csvg width='90' height='90' viewBox='0 0 90 90' xmlns='http://www.w3.org/2000/svg'%3E%3Cdefs%3E%3CradialGradient id='sporeGrad' cx='50%25' cy='50%25' r='50%25'%3E%3Cstop offset='0%25' style='stop-color:%23FFD700;stop-opacity:0.8'/%3E%3Cstop offset='70%25' style='stop-color:%23D4AF37;stop-opacity:0.4'/%3E%3Cstop offset='100%25' style='stop-color:%23B8860B;stop-opacity:0.1'/%3E%3C/radialGradient%3E%3C/defs%3E%3Cg fill='url(%23sporeGrad)' opacity='0.3'%3E%3Ccircle cx='12' cy='18' r='2' /%3E%3Ccircle cx='35' cy='28' r='1.2' /%3E%3Ccircle cx='58' cy='12' r='2.5' /%3E%3Ccircle cx='78' cy='38' r='1.8' /%3E%3Ccircle cx='18' cy='48' r='2.2' /%3E%3Ccircle cx='45' cy='58' r='1.5' /%3E%3Ccircle cx='72' cy='68' r='2' /%3E%3Ccircle cx='28' cy='78' r='1.8' /%3E%3Ccircle cx='8' cy='65' r='1.5' /%3E%3Ccircle cx='62' cy='82' r='2.2' /%3E%3C/g%3E%3Cg fill='%23FFD700' opacity='0.2'%3E%3Ccircle cx='25' cy='8' r='1' /%3E%3Ccircle cx='52' cy='25' r='0.8' /%3E%3Ccircle cx='75' cy='15' r='1.2' /%3E%3Ccircle cx='15' cy='35' r='0.9' /%3E%3Ccircle cx='40' cy='42' r='1.1' /%3E%3Ccircle cx='68' cy='52' r='0.7' /%3E%3Ccircle cx='85' cy='72' r='1' /%3E%3Ccircle cx='5' cy='85' r='0.8' /%3E%3Ccircle cx='48' cy='75' r='1.3' /%3E%3C/g%3E%3Cg fill='%23D4AF37' opacity='0.15'%3E%3Ccircle cx='32' cy='5' r='0.6' /%3E%3Ccircle cx='65' cy='20' r='0.8' /%3E%3Ccircle cx='22' cy='25' r='0.5' /%3E%3Ccircle cx='55' cy='35' r='0.7' /%3E%3Ccircle cx='82' cy='55' r='0.6' /%3E%3Ccircle cx='38' cy='65' r='0.8' /%3E%3Ccircle cx='12' cy='82' r='0.5' /%3E%3Ccircle cx='78' cy='85' r='0.7' /%3E%3C/g%3E%3C/svg%3E");
    animation: spore-float 35s linear infinite reverse;
    z-index: 2;
  }

  @keyframes mycelium-drift {
    0% { transform: translateX(0) translateY(0) rotate(0deg) scale(1); }
    25% { transform: translateX(-30px) translateY(-30px) rotate(90deg) scale(1.05); }
    50% { transform: translateX(-60px) translateY(-60px) rotate(180deg) scale(1); }
    75% { transform: translateX(-90px) translateY(-90px) rotate(270deg) scale(0.95); }
    100% { transform: translateX(-120px) translateY(-120px) rotate(360deg) scale(1); }
  }

  @keyframes spore-float {
    0% { transform: translateX(0) translateY(0) rotate(0deg) scale(1); }
    20% { transform: translateX(-20px) translateY(15px) rotate(72deg) scale(1.1); }
    40% { transform: translateX(-40px) translateY(30px) rotate(144deg) scale(0.9); }
    60% { transform: translateX(-60px) translateY(45px) rotate(216deg) scale(1.05); }
    80% { transform: translateX(-80px) translateY(60px) rotate(288deg) scale(0.95); }
    100% { transform: translateX(-90px) translateY(90px) rotate(360deg) scale(1); }
  }

  .quantum-hero-ultimate {
    position: relative;
    z-index: 10;
    text-align: center;
    padding: 2rem;
    max-width: 1200px;
    margin: 0 auto;
  }

  .quantum-title-ultimate {
    font-size: clamp(3.5rem, 10vw, 8rem);
    font-weight: 900;
    letter-spacing: -0.03em;
    line-height: 0.9;
    margin-bottom: 2rem;
    position: relative;
    background: linear-gradient(45deg, 
      #D4AF37 0%,
      #FFD700 25%,
      #FFF5DC 50%,
      #FFD700 75%,
      #D4AF37 100%);
    background-size: 400% 400%;
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    animation: quantum-ultimate-shimmer 4s ease-in-out infinite;
    filter: drop-shadow(0 0 30px rgba(212, 175, 55, 0.3));
  }

  .quantum-title-ultimate::before {
    content: attr(data-text);
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(45deg, 
      transparent 30%,
      rgba(255, 255, 255, 0.8) 50%,
      transparent 70%);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    animation: quantum-text-shine 3s ease-in-out infinite;
    z-index: 1;
  }

  @keyframes quantum-ultimate-shimmer {
    0%, 100% { 
      background-position: 0% 50%;
      filter: drop-shadow(0 0 30px rgba(212, 175, 55, 0.3)) hue-rotate(0deg);
    }
    25% { 
      background-position: 100% 50%;
      filter: drop-shadow(0 0 40px rgba(255, 215, 0, 0.4)) hue-rotate(15deg);
    }
    50% { 
      background-position: 50% 100%;
      filter: drop-shadow(0 0 50px rgba(255, 245, 220, 0.5)) hue-rotate(30deg);
    }
    75% { 
      background-position: 0% 0%;
      filter: drop-shadow(0 0 40px rgba(255, 215, 0, 0.4)) hue-rotate(15deg);
    }
  }

  @keyframes quantum-text-shine {
    0% { transform: translateX(-100%); opacity: 0; }
    50% { opacity: 1; }
    100% { transform: translateX(100%); opacity: 0; }
  }

  .quantum-subtitle-ultimate {
    font-size: clamp(1.4rem, 4vw, 2.2rem);
    color: rgba(255, 255, 255, 0.9);
    margin-bottom: 3rem;
    font-weight: 300;
    text-shadow: 0 0 20px rgba(255, 255, 255, 0.1);
    animation: quantum-text-glow 2s ease-in-out infinite alternate;
  }

  @keyframes quantum-text-glow {
    0% { text-shadow: 0 0 20px rgba(255, 255, 255, 0.1); }
    100% { text-shadow: 0 0 30px rgba(212, 175, 55, 0.2); }
  }

  .quantum-actions-ultimate {
    display: flex;
    gap: 2rem;
    justify-content: center;
    flex-wrap: wrap;
    margin-bottom: 4rem;
  }

  .quantum-btn-ultimate {
    padding: 1.5rem 3rem;
    font-size: 1.1rem;
    font-weight: 600;
    border-radius: 50px;
    text-decoration: none;
    position: relative;
    overflow: hidden;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.1);
  }

  .quantum-btn-ultimate::before {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    width: 0;
    height: 0;
    background: radial-gradient(circle, rgba(255, 255, 255, 0.3) 0%, transparent 70%);
    transform: translate(-50%, -50%);
    transition: width 0.6s, height 0.6s;
    border-radius: 50%;
  }

  .quantum-btn-ultimate:hover::before {
    width: 300px;
    height: 300px;
  }

  .quantum-btn-primary-ultimate {
    background: linear-gradient(135deg, #D4AF37 0%, #B8943F 50%, #D4AF37 100%);
    color: #000;
    box-shadow: 0 10px 30px rgba(212, 175, 55, 0.3);
  }

  .quantum-btn-primary-ultimate:hover {
    transform: translateY(-5px);
    box-shadow: 0 20px 40px rgba(212, 175, 55, 0.4);
  }

  .quantum-btn-secondary-ultimate {
    background: rgba(255, 255, 255, 0.05);
    color: #FFF5DC;
    border: 2px solid #D4AF37;
  }

  .quantum-btn-secondary-ultimate:hover {
    transform: translateY(-3px);
    background: rgba(212, 175, 55, 0.1);
    box-shadow: 0 15px 35px rgba(212, 175, 55, 0.2);
  }

  .quantum-neural-bg {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    opacity: 0.1;
    z-index: 1;
  }

  .quantum-floating-elements {
    position: absolute;
    width: 100%;
    height: 100%;
    pointer-events: none;
    z-index: 5;
  }

  .quantum-orb {
    position: absolute;
    border-radius: 50%;
    background: radial-gradient(circle at 30% 30%, rgba(212, 175, 55, 0.8), transparent 70%);
    filter: blur(1px);
    animation: quantum-float-orb 8s ease-in-out infinite;
  }

  .quantum-orb:nth-child(1) {
    width: 80px;
    height: 80px;
    top: 20%;
    left: 10%;
    animation-delay: 0s;
  }

  .quantum-orb:nth-child(2) {
    width: 60px;
    height: 60px;
    top: 60%;
    right: 15%;
    animation-delay: -2s;
  }

  .quantum-orb:nth-child(3) {
    width: 100px;
    height: 100px;
    bottom: 20%;
    left: 20%;
    animation-delay: -4s;
  }

  @keyframes quantum-float-orb {
    0%, 100% { transform: translateY(0) rotate(0deg); opacity: 0.3; }
    25% { transform: translateY(-30px) rotate(90deg); opacity: 0.6; }
    50% { transform: translateY(0) rotate(180deg); opacity: 0.3; }
    75% { transform: translateY(30px) rotate(270deg); opacity: 0.6; }
  }

  @media (max-width: 768px) {
    .quantum-actions-ultimate {
      flex-direction: column;
      align-items: center;
    }

    .quantum-btn-ultimate {
      width: 100%;
      max-width: 320px;
    }
  }
{%- endstyle -%}

<section id="quantum-hero-ultimate-{{ section.id }}">
  <!-- Neural Network Background -->
  <canvas class="quantum-neural-bg" id="neural-{{ section.id }}"></canvas>
  
  <!-- Floating Orbs -->
  <div class="quantum-floating-elements">
    <div class="quantum-orb"></div>
    <div class="quantum-orb"></div>
    <div class="quantum-orb"></div>
  </div>
  
  <!-- Crowe AI Avatar with Voice Integration -->
  <div class="crowe-ai-avatar-container" id="croweAvatarContainer">
    <div class="avatar-hologram-effect">
      <img src="{{ 'crowe-avatar.png' | asset_url }}" 
           alt="Crowe Logic AI Avatar" 
           class="crowe-ai-avatar" 
           id="croweAvatar"
           loading="eager">
      <div class="avatar-voice-visualizer" id="avatarVoiceVisualizer">
        <div class="voice-wave"></div>
        <div class="voice-wave"></div>
        <div class="voice-wave"></div>
        <div class="voice-wave"></div>
        <div class="voice-wave"></div>
      </div>
      <div class="avatar-status-indicator" id="avatarStatus">
        <span class="status-text">AI Assistant Ready</span>
      </div>
    </div>
  </div>

  <!-- Main Content -->
  <div class="quantum-hero-ultimate">
    <!-- AI Introduction Panel -->
    <div class="ai-intro-panel" id="aiIntroPanel">
      <div class="ai-speech-bubble">
        <p class="ai-message" id="aiMessage">
          👋 Hello! I'm Crowe Logic AI. I can help you discover our premium mushroom supplements and answer any questions about our products. 
          <strong>Click the microphone to start talking with me!</strong>
        </p>
      </div>
    </div>

    <h1 class="quantum-title-ultimate" data-text="{{ section.settings.title | default: 'Meet Crowe Logic AI' }}">
      {{ section.settings.title | default: 'Meet Crowe Logic AI' }}
    </h1>
    
    <p class="quantum-subtitle-ultimate">
      {{ section.settings.subtitle | default: 'Your intelligent mushroom supplement advisor with voice AI technology' }}
    </p>

    <!-- Voice Interaction Controls -->
    <div class="voice-controls" id="voiceControls">
      <button class="voice-btn" id="voiceBtn" title="Talk to Crowe AI">
        <svg class="voice-icon" viewBox="0 0 24 24" fill="none">
          <path d="M12 1a3 3 0 0 0-3 3v8a3 3 0 0 0 6 0V4a3 3 0 0 0-3-3z" stroke="currentColor" stroke-width="2"/>
          <path d="M19 10v2a7 7 0 0 1-14 0v-2" stroke="currentColor" stroke-width="2"/>
          <line x1="12" y1="19" x2="12" y2="23" stroke="currentColor" stroke-width="2"/>
          <line x1="8" y1="23" x2="16" y2="23" stroke="currentColor" stroke-width="2"/>
        </svg>
        <span class="voice-btn-text">Start Voice Chat</span>
      </button>
      
      <div class="voice-status" id="voiceStatus">
        <div class="listening-indicator">
          <div class="pulse-ring"></div>
          <div class="pulse-ring"></div>
          <div class="pulse-ring"></div>
        </div>
        <span class="status-text">Ready to listen...</span>
      </div>
    </div>
    
    <div class="quantum-actions-ultimate">
      <a href="{{ section.settings.primary_link | default: '/collections/all' }}" class="quantum-btn-ultimate quantum-btn-primary-ultimate">
        {{ section.settings.primary_text | default: 'Shop Supplements' }}
      </a>
      <a href="{{ section.settings.secondary_link | default: '/pages/about' }}" class="quantum-btn-ultimate quantum-btn-secondary-ultimate">
        {{ section.settings.secondary_text | default: 'Learn More' }}
      </a>
    </div>

    <!-- Product Showcase Cards -->
    <div class="hero-product-showcase" id="heroProductShowcase">
      <div class="showcase-card">
        <div class="card-glow"></div>
        <h3>🦁 Lion's Mane</h3>
        <p>Cognitive Enhancement</p>
      </div>
      <div class="showcase-card">
        <div class="card-glow"></div>
        <h3>🍄 Reishi</h3>
        <p>Stress Relief</p>
      </div>
      <div class="showcase-card">
        <div class="card-glow"></div>
        <h3>⚡ Cordyceps</h3>
        <p>Energy Boost</p>
      </div>
    </div>
  </div>
</section>

<script>
(function() {
  'use strict';

  // ===== ADVANCED NEURAL NETWORK CANVAS =====
  const canvas = document.getElementById('neural-{{ section.id }}');
  if (canvas) {
    const ctx = canvas.getContext('2d');
    let animationId;
    
    function resizeCanvas() {
      canvas.width = canvas.offsetWidth;
      canvas.height = canvas.offsetHeight;
    }
    
    resizeCanvas();
    window.addEventListener('resize', resizeCanvas);
    
    // Enhanced neural network nodes
    const nodes = [];
    const nodeCount = 80;
    
    for (let i = 0; i < nodeCount; i++) {
      nodes.push({
        x: Math.random() * canvas.width,
        y: Math.random() * canvas.height,
        vx: (Math.random() - 0.5) * 0.3,
        vy: (Math.random() - 0.5) * 0.3,
        radius: Math.random() * 3 + 1,
        opacity: Math.random() * 0.6 + 0.2,
        pulsePhase: Math.random() * Math.PI * 2
      });
    }
    
    let isAnimating = true;
    
    function animate() {
      if (!isAnimating) return;
      
      ctx.clearRect(0, 0, canvas.width, canvas.height);
      
      // Time for pulsing effects
      const time = Date.now() * 0.001;
      
      nodes.forEach((node, i) => {
        // Update position
        node.x += node.vx;
        node.y += node.vy;
        
        // Bounce off edges with energy conservation
        if (node.x < 0 || node.x > canvas.width) {
          node.vx *= -0.8;
          node.x = Math.max(0, Math.min(canvas.width, node.x));
        }
        if (node.y < 0 || node.y > canvas.height) {
          node.vy *= -0.8;
          node.y = Math.max(0, Math.min(canvas.height, node.y));
        }
        
        // Pulsing radius effect
        const pulseRadius = node.radius + Math.sin(time + node.pulsePhase) * 0.5;
        const pulseOpacity = node.opacity + Math.sin(time * 2 + node.pulsePhase) * 0.2;
        
        // Draw node with glow effect
        ctx.beginPath();
        ctx.arc(node.x, node.y, pulseRadius, 0, Math.PI * 2);
        
        // Create gradient for glow
        const gradient = ctx.createRadialGradient(
          node.x, node.y, 0,
          node.x, node.y, pulseRadius * 3
        );
        gradient.addColorStop(0, `rgba(212, 175, 55, ${pulseOpacity})`);
        gradient.addColorStop(0.5, `rgba(212, 175, 55, ${pulseOpacity * 0.3})`);
        gradient.addColorStop(1, 'transparent');
        
        ctx.fillStyle = gradient;
        ctx.fill();
        
        // Draw connections with quantum effects
        nodes.slice(i + 1).forEach(otherNode => {
          const dx = node.x - otherNode.x;
          const dy = node.y - otherNode.y;
          const distance = Math.sqrt(dx * dx + dy * dy);
          
          if (distance < 120) {
            ctx.beginPath();
            ctx.moveTo(node.x, node.y);
            ctx.lineTo(otherNode.x, otherNode.y);
            
            const connectionOpacity = (1 - distance / 120) * 0.3;
            const hue = (time * 50 + distance) % 360;
            ctx.strokeStyle = `hsla(${hue}, 70%, 60%, ${connectionOpacity})`;
            ctx.lineWidth = 1 + Math.sin(time * 3 + distance * 0.01) * 0.5;
            ctx.stroke();
          }
        });
      });
      
      animationId = requestAnimationFrame(animate);
    }
    
    animate();
    
    document.addEventListener('visibilitychange', () => {
      isAnimating = !document.hidden;
      if (isAnimating) animate();
    });
  }

  // ===== CROWE AI VOICE INTEGRATION =====
  class CroweVoiceAI {
    constructor() {
      this.isInitialized = false;
      this.isListening = false;
      this.isSpeaking = false;
      this.conversationId = null;
      
      // ElevenLabs Configuration
      this.config = {
        apiKey: 'sk_070497763bb534837e3810ba7c07578ae06fbc1e3760a3b7',
        voiceId: 'BsxQGfHOT8xeJhwW3B2u',
        agentId: 'agent_01jz4jgxksfj7v6tkc6f1g7d9y',
        apiUrl: 'https://api.elevenlabs.io/v1'
      };
      
      this.init();
    }

    async init() {
      try {
        this.setupEventListeners();
        this.setupVoiceVisualizer();
        this.setupAvatarAnimations();
        this.initializeIntroMessage();
        this.isInitialized = true;
        console.log('🎤 Crowe Voice AI initialized successfully!');
      } catch (error) {
        console.error('Failed to initialize Crowe Voice AI:', error);
      }
    }

    setupEventListeners() {
      const voiceBtn = document.getElementById('voiceBtn');
      const avatar = document.getElementById('croweAvatar');
      
      if (voiceBtn) {
        voiceBtn.addEventListener('click', () => this.toggleVoiceChat());
      }
      
      if (avatar) {
        avatar.addEventListener('click', () => this.toggleVoiceChat());
        avatar.style.cursor = 'pointer';
      }
    }

    setupVoiceVisualizer() {
      const visualizer = document.getElementById('avatarVoiceVisualizer');
      if (!visualizer) return;

      // Create voice wave animation
      const waves = visualizer.querySelectorAll('.voice-wave');
      waves.forEach((wave, index) => {
        wave.style.animationDelay = `${index * 0.1}s`;
      });
    }

    setupAvatarAnimations() {
      const avatar = document.getElementById('croweAvatar');
      if (!avatar) return;

      // Add breathing animation to avatar
      avatar.style.animation = 'avatarBreathe 4s ease-in-out infinite';
      
      // Add CSS for avatar animations
      if (!document.querySelector('#avatar-animations')) {
        const style = document.createElement('style');
        style.id = 'avatar-animations';
        style.textContent = `
          @keyframes avatarBreathe {
            0%, 100% { transform: scale(1) rotate(0deg); filter: drop-shadow(0 0 20px rgba(212, 175, 55, 0.3)); }
            50% { transform: scale(1.05) rotate(1deg); filter: drop-shadow(0 0 30px rgba(212, 175, 55, 0.6)); }
          }
          
          @keyframes avatarSpeak {
            0%, 100% { transform: scale(1) rotate(0deg); filter: hue-rotate(0deg) drop-shadow(0 0 25px rgba(212, 175, 55, 0.4)); }
            25% { transform: scale(1.08) rotate(-1deg); filter: hue-rotate(10deg) drop-shadow(0 0 35px rgba(93, 173, 226, 0.5)); }
            75% { transform: scale(1.08) rotate(1deg); filter: hue-rotate(-10deg) drop-shadow(0 0 35px rgba(255, 215, 0, 0.5)); }
          }
          
          @keyframes voiceWave {
            0%, 100% { height: 4px; opacity: 0.3; }
            50% { height: 20px; opacity: 1; }
          }
          
          .voice-wave {
            background: linear-gradient(45deg, #D4AF37, #5dade2);
            width: 3px;
            margin: 0 2px;
            border-radius: 2px;
            animation: voiceWave 1s ease-in-out infinite;
          }
          
          .avatar-speaking .voice-wave {
            animation-duration: 0.5s;
          }
          
          .listening-indicator .pulse-ring {
            border: 3px solid #D4AF37;
            border-radius: 50%;
            height: 60px;
            width: 60px;
            position: absolute;
            left: 50%;
            top: 50%;
            transform: translate(-50%, -50%);
            animation: pulse-ring 1.5s cubic-bezier(0.215, 0.61, 0.355, 1) infinite;
          }
          
          .pulse-ring:nth-child(2) { animation-delay: 0.5s; }
          .pulse-ring:nth-child(3) { animation-delay: 1s; }
          
          @keyframes pulse-ring {
            0% { transform: translate(-50%, -50%) scale(0.33); opacity: 1; }
            80%, 100% { transform: translate(-50%, -50%) scale(2.4); opacity: 0; }
          }
        `;
        document.head.appendChild(style);
      }
    }

    async toggleVoiceChat() {
      if (this.isListening) {
        this.stopListening();
      } else {
        await this.startListening();
      }
    }

    async startListening() {
      try {
        const stream = await navigator.mediaDevices.getUserMedia({ audio: true });
        
        this.isListening = true;
        this.updateUI('listening');
        
        // Initialize speech recognition
        if ('webkitSpeechRecognition' in window) {
          const recognition = new webkitSpeechRecognition();
          recognition.continuous = true;
          recognition.interimResults = true;
          recognition.lang = 'en-US';
          
          recognition.onresult = (event) => {
            const transcript = Array.from(event.results)
              .map(result => result[0].transcript)
              .join('');
            
            if (event.results[event.results.length - 1].isFinal) {
              this.processSpeech(transcript);
            }
          };
          
          recognition.onerror = (event) => {
            console.error('Speech recognition error:', event.error);
            this.stopListening();
          };
          
          recognition.start();
          this.recognition = recognition;
        } else {
          // Fallback for browsers without speech recognition
          this.simulateConversation();
        }
        
      } catch (error) {
        console.error('Microphone access denied:', error);
        this.showError('Microphone access required for voice chat');
      }
    }

    stopListening() {
      this.isListening = false;
      if (this.recognition) {
        this.recognition.stop();
      }
      this.updateUI('ready');
    }

    async processSpeech(transcript) {
      console.log('User said:', transcript);
      
      this.updateUI('processing');
      this.updateAIMessage('Processing your request...');
      
      try {
        // Call ElevenLabs Conversational AI API
        const response = await this.callElevenLabsAPI(transcript);
        
        if (response.audio) {
          await this.playAIResponse(response.audio);
        }
        
        if (response.text) {
          this.updateAIMessage(response.text);
        }
        
      } catch (error) {
        console.error('AI processing error:', error);
        this.simulateAIResponse(transcript);
      }
      
      this.updateUI('ready');
    }

    async callElevenLabsAPI(userMessage) {
      try {
        // Start conversation if needed
        if (!this.conversationId) {
          const startResponse = await fetch(`${this.config.apiUrl}/convai/conversations`, {
            method: 'POST',
            headers: {
              'Authorization': `Bearer ${this.config.apiKey}`,
              'Content-Type': 'application/json'
            },
            body: JSON.stringify({
              agent_id: this.config.agentId
            })
          });
          
          const startData = await startResponse.json();
          this.conversationId = startData.conversation_id;
        }
        
        // Send message to conversation
        const response = await fetch(`${this.config.apiUrl}/convai/conversations/${this.conversationId}`, {
          method: 'POST',
          headers: {
            'Authorization': `Bearer ${this.config.apiKey}`,
            'Content-Type': 'application/json'
          },
          body: JSON.stringify({
            text: userMessage,
            mode: 'voice'
          })
        });
        
        if (!response.ok) {
          throw new Error(`API call failed: ${response.status}`);
        }
        
        const audioBlob = await response.blob();
        return { audio: audioBlob };
        
      } catch (error) {
        console.error('ElevenLabs API error:', error);
        throw error;
      }
    }

    async playAIResponse(audioBlob) {
      this.isSpeaking = true;
      this.updateUI('speaking');
      
      const audioUrl = URL.createObjectURL(audioBlob);
      const audio = new Audio(audioUrl);
      
      audio.onended = () => {
        this.isSpeaking = false;
        this.updateUI('ready');
        URL.revokeObjectURL(audioUrl);
      };
      
      await audio.play();
    }

    simulateAIResponse(userInput) {
      const responses = {
        'hello': "Hello! I'm Crowe Logic AI, your personal mushroom supplement advisor. How can I help you today?",
        'products': "We offer premium mushroom supplements including Lion's Mane for cognitive enhancement, Reishi for stress relief, and Cordyceps for energy boost.",
        'lions mane': "Lion's Mane is excellent for cognitive function, memory, and focus. It contains compounds that support nerve growth factor production.",
        'reishi': "Reishi is known as the 'mushroom of immortality' and is perfect for stress relief, immune support, and better sleep quality.",
        'cordyceps': "Cordyceps is fantastic for natural energy, athletic performance, and respiratory health. It's great for active lifestyles!",
        'default': "That's interesting! I'd be happy to help you learn more about our mushroom supplements. What specific benefits are you looking for?"
      };
      
      const lowerInput = userInput.toLowerCase();
      let response = responses.default;
      
      for (const [key, value] of Object.entries(responses)) {
        if (lowerInput.includes(key)) {
          response = value;
          break;
        }
      }
      
      this.updateAIMessage(response);
      this.simulateVoiceResponse(response);
    }

    simulateVoiceResponse(text) {
      this.isSpeaking = true;
      this.updateUI('speaking');
      
      // Simulate speech duration based on text length
      const duration = Math.max(2000, text.length * 50);
      
      setTimeout(() => {
        this.isSpeaking = false;
        this.updateUI('ready');
      }, duration);
    }

    simulateConversation() {
      // Fallback for browsers without speech recognition
      const defaultMessage = "I hear you! Unfortunately, your browser doesn't support speech recognition, but I'm still here to help. Try clicking on our product cards below to learn more!";
      this.updateAIMessage(defaultMessage);
      this.simulateVoiceResponse(defaultMessage);
    }

    updateUI(state) {
      const voiceBtn = document.getElementById('voiceBtn');
      const voiceStatus = document.getElementById('voiceStatus');
      const avatarContainer = document.getElementById('croweAvatarContainer');
      const avatar = document.getElementById('croweAvatar');
      
      if (!voiceBtn || !voiceStatus) return;
      
      // Reset classes
      avatarContainer?.classList.remove('avatar-listening', 'avatar-speaking', 'avatar-processing');
      
      switch (state) {
        case 'listening':
          voiceBtn.querySelector('.voice-btn-text').textContent = 'Listening...';
          voiceStatus.querySelector('.status-text').textContent = 'I\'m listening...';
          avatarContainer?.classList.add('avatar-listening');
          avatar.style.animation = 'avatarSpeak 1s ease-in-out infinite';
          break;
          
        case 'processing':
          voiceBtn.querySelector('.voice-btn-text').textContent = 'Processing...';
          voiceStatus.querySelector('.status-text').textContent = 'Thinking...';
          avatarContainer?.classList.add('avatar-processing');
          break;
          
        case 'speaking':
          voiceBtn.querySelector('.voice-btn-text').textContent = 'Speaking...';
          voiceStatus.querySelector('.status-text').textContent = 'I\'m responding...';
          avatarContainer?.classList.add('avatar-speaking');
          avatar.style.animation = 'avatarSpeak 0.5s ease-in-out infinite';
          break;
          
        default: // ready
          voiceBtn.querySelector('.voice-btn-text').textContent = 'Start Voice Chat';
          voiceStatus.querySelector('.status-text').textContent = 'Ready to listen...';
          avatar.style.animation = 'avatarBreathe 4s ease-in-out infinite';
          break;
      }
    }

    updateAIMessage(message) {
      const aiMessage = document.getElementById('aiMessage');
      if (aiMessage) {
        aiMessage.innerHTML = message;
        
        // Add typing effect
        this.typewriterEffect(aiMessage, message);
      }
    }

    typewriterEffect(element, text) {
      element.innerHTML = '';
      let i = 0;
      const timer = setInterval(() => {
        if (i < text.length) {
          element.innerHTML += text.charAt(i);
          i++;
        } else {
          clearInterval(timer);
        }
      }, 30);
    }

    initializeIntroMessage() {
      setTimeout(() => {
        const introPanel = document.getElementById('aiIntroPanel');
        if (introPanel) {
          introPanel.style.animation = 'fadeInUp 1s ease-out';
        }
      }, 1000);
    }

    showError(message) {
      this.updateAIMessage(`⚠️ ${message}`);
      this.updateUI('ready');
    }
  }

  // ===== PRODUCT SHOWCASE ANIMATIONS =====
  function initializeProductShowcase() {
    const showcaseCards = document.querySelectorAll('.showcase-card');
    
    showcaseCards.forEach((card, index) => {
      card.style.animationDelay = `${index * 0.2}s`;
      
      card.addEventListener('mouseenter', () => {
        card.style.transform = 'translateY(-10px) rotateY(5deg) scale(1.05)';
        card.querySelector('.card-glow').style.opacity = '1';
      });
      
      card.addEventListener('mouseleave', () => {
        card.style.transform = 'translateY(0) rotateY(0deg) scale(1)';
        card.querySelector('.card-glow').style.opacity = '0.5';
      });
    });
  }

  // ===== INITIALIZE EVERYTHING =====
  document.addEventListener('DOMContentLoaded', function() {
    // Initialize Crowe Voice AI
    window.croweVoiceAI = new CroweVoiceAI();
    
    // Initialize product showcase
    initializeProductShowcase();
    
    // Add scroll animations
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.style.animation = 'fadeInUp 1s ease-out forwards';
        }
      });
    });
    
    document.querySelectorAll('.showcase-card, .quantum-actions-ultimate').forEach(el => {
      observer.observe(el);
    });
    
    console.log('🌟 Quantum Hero Ultimate: All systems operational!');
  });

})();
</script>

{% schema %}
{
  "name": "Quantum Hero Ultimate",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Hero Title",
      "default": "Revolutionary Experience"
    },
    {
      "type": "textarea",
      "id": "subtitle",
      "label": "Hero Subtitle",
      "default": "Discover the future of e-commerce with cutting-edge design and immersive experiences"
    },
    {
      "type": "url",
      "id": "primary_link",
      "label": "Primary Button Link"
    },
    {
      "type": "text",
      "id": "primary_text",
      "label": "Primary Button Text",
      "default": "Experience the Future"
    },
    {
      "type": "url",
      "id": "secondary_link",
      "label": "Secondary Button Link"
    },
    {
      "type": "text",
      "id": "secondary_text",
      "label": "Secondary Button Text",
      "default": "Explore Collection"
    }
  ],
  "presets": [
    {
      "name": "Quantum Hero Ultimate"
    }
  ]
}
{% endschema %}

{%- comment -%}
🚀 Crowe Logic™ Ultra Collection - Maximum Conversion Design
Advanced product grid with AI-driven recommendations and social proof
{%- endcomment -%}

<style>
/* 🎯 Ultra Collection Container */
.crowe-collection-ultra {
  padding: 40px 0;
  background: linear-gradient(135deg, 
    rgba(244, 244, 242, 0.98) 0%,
    rgba(244, 244, 242, 1) 50%,
    rgba(244, 244, 242, 0.98) 100%
  );
  position: relative;
  overflow: hidden;
}

.crowe-collection-ultra::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: 
    radial-gradient(circle at 20% 20%, rgba(198, 163, 81, 0.03) 0%, transparent 50%),
    radial-gradient(circle at 80% 80%, rgba(201, 184, 138, 0.02) 0%, transparent 50%);
  pointer-events: none;
  z-index: 1;
}

/* 🎨 Collection Header */
.collection-header {
  text-align: center;
  margin-bottom: 60px;
  position: relative;
  z-index: 2;
}

.collection-title {
  font-size: 48px;
  font-weight: 700;
  color: var(--clr-accent);
  margin-bottom: 16px;
  background: linear-gradient(135deg, 
    var(--clr-accent) 0%,
    #E4CC88 50%,
    var(--clr-accent) 100%
  );
  background-size: 200% 200%;
  -webkit-background-clip: text;
  background-clip: text;
  -webkit-text-fill-color: transparent;
  animation: shimmer 4s ease-in-out infinite;
}

@keyframes shimmer {
  0%, 100% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
}

.collection-description {
  font-size: 18px;
  color: rgba(0, 0, 0, 0.7);
  max-width: 800px;
  margin: 0 auto 32px;
  line-height: 1.6;
}

.collection-stats {
  display: flex;
  justify-content: center;
  gap: 32px;
  margin-top: 32px;
  flex-wrap: wrap;
}

.collection-stat {
  text-align: center;
  padding: 16px 24px;
  background: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(198, 163, 81, 0.1);
  border-radius: 16px;
  backdrop-filter: blur(10px);
  transition: all 0.3s ease;
}

.collection-stat:hover {
  background: rgba(198, 163, 81, 0.1);
  border-color: rgba(198, 163, 81, 0.3);
  transform: translateY(-2px);
}

.collection-stat-number {
  font-size: 24px;
  font-weight: 700;
  color: var(--clr-accent);
  display: block;
}

.collection-stat-label {
  font-size: 14px;
  color: rgba(0, 0, 0, 0.6);
  font-weight: 600;
}

/* 🔥 Social Proof Banner */
.social-proof-banner {
  background: linear-gradient(135deg, 
    rgba(34, 197, 94, 0.1) 0%,
    rgba(21, 128, 61, 0.05) 100%
  );
  border: 1px solid rgba(34, 197, 94, 0.2);
  border-radius: 16px;
  padding: 20px;
  margin-bottom: 40px;
  position: relative;
  overflow: hidden;
}

.social-proof-banner::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, 
    transparent,
    rgba(34, 197, 94, 0.05),
    transparent
  );
  animation: socialProofSlide 3s ease-in-out infinite;
}

@keyframes socialProofSlide {
  0% { left: -100%; }
  50% { left: 100%; }
  100% { left: -100%; }
}

.social-proof-content {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 16px;
  position: relative;
  z-index: 2;
}

.social-proof-icon {
  width: 24px;
  height: 24px;
  color: rgba(34, 197, 94, 0.8);
}

.social-proof-text {
  font-size: 16px;
  font-weight: 600;
  color: rgba(34, 197, 94, 0.9);
}

.social-proof-count {
  font-weight: 700;
  color: rgba(34, 197, 94, 1);
}

/* 🎯 Conversion Triggers */
.conversion-triggers {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 24px;
  margin-bottom: 40px;
}

.conversion-trigger {
  background: linear-gradient(135deg, 
    rgba(198, 163, 81, 0.05) 0%,
    rgba(201, 184, 138, 0.05) 100%
  );
  border: 1px solid rgba(198, 163, 81, 0.1);
  border-radius: 16px;
  padding: 24px;
  text-align: center;
  transition: all 0.3s ease;
  position: relative;
  overflow: hidden;
}

.conversion-trigger::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(45deg, 
    transparent 0%,
    rgba(198, 163, 81, 0.02) 50%,
    transparent 100%
  );
  opacity: 0;
  transition: opacity 0.3s ease;
}

.conversion-trigger:hover::before {
  opacity: 1;
}

.conversion-trigger:hover {
  border-color: rgba(198, 163, 81, 0.3);
  transform: translateY(-4px);
  box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
}

.conversion-trigger-icon {
  width: 48px;
  height: 48px;
  margin: 0 auto 16px;
  color: var(--clr-accent);
}

.conversion-trigger-title {
  font-size: 18px;
  font-weight: 700;
  color: var(--clr-accent);
  margin-bottom: 8px;
}

.conversion-trigger-text {
  font-size: 14px;
  color: rgba(0, 0, 0, 0.7);
  line-height: 1.5;
}

/* 🗂️ Collection Layout */
.collection-layout {
  display: grid;
  grid-template-columns: 1fr 3fr;
  gap: 40px;
  max-width: 1400px;
  margin: 0 auto;
  padding: 0 20px;
  position: relative;
  z-index: 2;
}

.collection-sidebar {
  position: sticky;
  top: 120px;
  height: fit-content;
}

.collection-main {
  min-height: 800px;
}

/* 🎮 Sort & View Controls */
.collection-controls {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 32px;
  padding: 20px;
  background: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(198, 163, 81, 0.1);
  border-radius: 16px;
  backdrop-filter: blur(10px);
}

.collection-sort {
  display: flex;
  align-items: center;
  gap: 16px;
}

.collection-sort-label {
  font-size: 14px;
  font-weight: 600;
  color: rgba(0, 0, 0, 0.7);
}

.collection-sort-select {
  padding: 8px 16px;
  border: 2px solid rgba(198, 163, 81, 0.2);
  border-radius: 12px;
  background: rgba(255, 255, 255, 0.9);
  color: var(--clr-accent);
  font-weight: 600;
  cursor: pointer;
  transition: all 0.3s ease;
}

.collection-sort-select:hover,
.collection-sort-select:focus {
  border-color: rgba(198, 163, 81, 0.4);
  outline: none;
  box-shadow: 0 4px 15px rgba(198, 163, 81, 0.2);
}

.collection-view-toggle {
  display: flex;
  gap: 8px;
}

.view-toggle-btn {
  padding: 8px 12px;
  border: 2px solid rgba(198, 163, 81, 0.2);
  border-radius: 12px;
  background: rgba(255, 255, 255, 0.9);
  color: var(--clr-accent);
  cursor: pointer;
  transition: all 0.3s ease;
}

.view-toggle-btn:hover,
.view-toggle-btn.active {
  background: var(--clr-accent);
  color: var(--clr-bg-dark);
  border-color: var(--clr-accent);
}

.view-toggle-btn svg {
  width: 20px;
  height: 20px;
}

/* 🎨 Product Grid */
.collection-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
  gap: 32px;
  margin-bottom: 60px;
}

.collection-grid.list-view {
  grid-template-columns: 1fr;
}

.collection-grid.list-view .crowe-product-card-ultra {
  display: grid;
  grid-template-columns: 300px 1fr;
  max-width: none;
  height: 200px;
}

.collection-grid.list-view .card-media-container {
  border-radius: 20px 0 0 20px;
}

.collection-grid.list-view .card-content {
  display: flex;
  flex-direction: column;
  justify-content: center;
  padding: 32px;
}

/* 📊 Performance Metrics */
.collection-metrics {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 24px;
  margin-bottom: 40px;
  padding: 32px;
  background: linear-gradient(135deg, 
    rgba(23, 20, 30, 0.05) 0%,
    rgba(198, 163, 81, 0.02) 100%
  );
  border: 1px solid rgba(198, 163, 81, 0.1);
  border-radius: 20px;
}

.collection-metric {
  text-align: center;
  padding: 20px;
  background: rgba(255, 255, 255, 0.8);
  border-radius: 16px;
  transition: all 0.3s ease;
}

.collection-metric:hover {
  transform: translateY(-4px);
  box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
}

.collection-metric-value {
  font-size: 28px;
  font-weight: 700;
  color: var(--clr-accent);
  display: block;
  margin-bottom: 8px;
}

.collection-metric-label {
  font-size: 14px;
  color: rgba(0, 0, 0, 0.6);
  font-weight: 600;
}

/* 🔄 Loading States */
.collection-loading {
  text-align: center;
  padding: 60px 20px;
}

.collection-loading-spinner {
  width: 64px;
  height: 64px;
  border: 4px solid rgba(198, 163, 81, 0.1);
  border-top: 4px solid var(--clr-accent);
  border-radius: 50%;
  animation: spin 1s linear infinite;
  margin: 0 auto 24px;
}

@keyframes spin {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}

.collection-loading-text {
  font-size: 18px;
  color: rgba(0, 0, 0, 0.6);
  font-weight: 600;
}

/* 📱 Mobile Responsive */
@media (max-width: 1024px) {
  .collection-layout {
    grid-template-columns: 1fr;
    gap: 32px;
  }
  
  .collection-sidebar {
    position: relative;
    top: 0;
    order: -1;
  }
  
  .collection-controls {
    flex-direction: column;
    gap: 16px;
    align-items: stretch;
  }
  
  .collection-sort {
    justify-content: center;
  }
  
  .collection-view-toggle {
    justify-content: center;
  }
}

@media (max-width: 768px) {
  .collection-title {
    font-size: 32px;
  }
  
  .collection-stats {
    grid-template-columns: repeat(2, 1fr);
  }
  
  .collection-grid {
    grid-template-columns: 1fr;
    gap: 24px;
  }
  
  .conversion-triggers {
    grid-template-columns: 1fr;
  }
  
  .collection-metrics {
    grid-template-columns: repeat(2, 1fr);
  }
}

@media (max-width: 480px) {
  .collection-stats {
    grid-template-columns: 1fr;
  }
  
  .collection-metrics {
    grid-template-columns: 1fr;
  }
  
  .collection-grid.list-view .crowe-product-card-ultra {
    grid-template-columns: 1fr;
    height: auto;
  }
  
  .collection-grid.list-view .card-media-container {
    border-radius: 20px 20px 0 0;
  }
}

/* 🎯 Urgency Indicators */
.urgency-banner {
  position: fixed;
  bottom: 20px;
  right: 20px;
  background: linear-gradient(135deg, 
    rgba(239, 68, 68, 0.95) 0%,
    rgba(185, 28, 28, 0.9) 100%
  );
  color: white;
  padding: 16px 24px;
  border-radius: 16px;
  font-weight: 600;
  z-index: 1000;
  box-shadow: 0 8px 25px rgba(239, 68, 68, 0.3);
  animation: urgencyPulse 2s ease-in-out infinite;
  backdrop-filter: blur(10px);
}

@keyframes urgencyPulse {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.05); }
}

.urgency-banner-close {
  position: absolute;
  top: 8px;
  right: 8px;
  background: none;
  border: none;
  color: white;
  font-size: 18px;
  cursor: pointer;
  padding: 4px;
  border-radius: 4px;
  transition: background-color 0.3s ease;
}

.urgency-banner-close:hover {
  background: rgba(255, 255, 255, 0.2);
}
</style>

<section class="crowe-collection-ultra">
  <!-- Collection Header -->
  <div class="collection-header">
    <h1 class="collection-title">
      {{ collection.title | default: 'Premium Collection' }}
    </h1>
    
    {%- if collection.description != blank -%}
      <div class="collection-description">
        {{ collection.description }}
      </div>
    {%- endif -%}
    
    <!-- Collection Stats -->
    <div class="collection-stats">
      <div class="collection-stat">
        <span class="collection-stat-number">{{ collection.products_count }}</span>
        <span class="collection-stat-label">Products</span>
      </div>
      <div class="collection-stat">
        <span class="collection-stat-number">4.8★</span>
        <span class="collection-stat-label">Average Rating</span>
      </div>
      <div class="collection-stat">
        <span class="collection-stat-number">10K+</span>
        <span class="collection-stat-label">Happy Growers</span>
      </div>
      <div class="collection-stat">
        <span class="collection-stat-number">24/7</span>
        <span class="collection-stat-label">Expert Support</span>
      </div>
    </div>
  </div>

  <!-- Social Proof Banner -->
  <div class="social-proof-banner">
    <div class="social-proof-content">
      <svg class="social-proof-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor">
        <path d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
      </svg>
      <div class="social-proof-text">
        <span class="social-proof-count">1,247</span> people purchased from this collection in the last 30 days
      </div>
    </div>
  </div>

  <!-- Conversion Triggers -->
  <div class="conversion-triggers">
    <div class="conversion-trigger">
      <svg class="conversion-trigger-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor">
        <path d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
      </svg>
      <div class="conversion-trigger-title">100% Organic</div>
      <div class="conversion-trigger-text">All products are certified organic and sustainably sourced</div>
    </div>
    
    <div class="conversion-trigger">
      <svg class="conversion-trigger-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor">
        <path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z"></path>
      </svg>
      <div class="conversion-trigger-title">Fast Results</div>
      <div class="conversion-trigger-text">See your first harvest in as little as 7-14 days</div>
    </div>
    
    <div class="conversion-trigger">
      <svg class="conversion-trigger-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor">
        <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"></path>
      </svg>
      <div class="conversion-trigger-title">Expert Approved</div>
      <div class="conversion-trigger-text">Recommended by professional mycologists worldwide</div>
    </div>
  </div>

  <!-- Collection Layout -->
  <div class="collection-layout">
    
    <!-- Sidebar with Smart Filter -->
    <div class="collection-sidebar">
      {%- render 'crowe-smart-filter' -%}
    </div>
    
    <!-- Main Collection Area -->
    <div class="collection-main">
      
      <!-- Collection Controls -->
      <div class="collection-controls">
        <div class="collection-sort">
          <label class="collection-sort-label">Sort by:</label>
          <select class="collection-sort-select" onchange="sortProducts(this.value)">
            <option value="manual">Featured</option>
            <option value="price-ascending">Price: Low to High</option>
            <option value="price-descending">Price: High to Low</option>
            <option value="title-ascending">Name: A-Z</option>
            <option value="title-descending">Name: Z-A</option>
            <option value="created-ascending">Oldest First</option>
            <option value="created-descending">Newest First</option>
            <option value="best-selling">Best Selling</option>
          </select>
        </div>
        
        <div class="collection-view-toggle">
          <button class="view-toggle-btn active" onclick="setGridView('grid')" title="Grid View">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
              <rect x="3" y="3" width="7" height="7"></rect>
              <rect x="14" y="3" width="7" height="7"></rect>
              <rect x="14" y="14" width="7" height="7"></rect>
              <rect x="3" y="14" width="7" height="7"></rect>
            </svg>
          </button>
          <button class="view-toggle-btn" onclick="setGridView('list')" title="List View">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
              <line x1="8" y1="6" x2="21" y2="6"></line>
              <line x1="8" y1="12" x2="21" y2="12"></line>
              <line x1="8" y1="18" x2="21" y2="18"></line>
              <line x1="3" y1="6" x2="3.01" y2="6"></line>
              <line x1="3" y1="12" x2="3.01" y2="12"></line>
              <line x1="3" y1="18" x2="3.01" y2="18"></line>
            </svg>
          </button>
        </div>
      </div>
      
      <!-- Performance Metrics -->
      <div class="collection-metrics">
        <div class="collection-metric">
          <span class="collection-metric-value">97%</span>
          <span class="collection-metric-label">Success Rate</span>
        </div>
        <div class="collection-metric">
          <span class="collection-metric-value">15K+</span>
          <span class="collection-metric-label">Reviews</span>
        </div>
        <div class="collection-metric">
          <span class="collection-metric-value">48hrs</span>
          <span class="collection-metric-label">Fast Shipping</span>
        </div>
        <div class="collection-metric">
          <span class="collection-metric-value">30 Days</span>
          <span class="collection-metric-label">Guarantee</span>
        </div>
      </div>
      
      <!-- Product Grid -->
      <div class="collection-grid" id="productGrid">
        {%- for product in collection.products -%}
          <div class="product-item" 
               data-product-id="{{ product.id }}"
               data-price="{{ product.price }}"
               data-difficulty="{{ product.metafields.custom.difficulty.value | default: 'beginner' }}"
               data-type="{{ product.type | default: 'growing-kit' }}"
               data-season="{{ product.metafields.custom.season.value | default: 'year-round' }}">
            {%- render 'crowe-product-card-ultra', 
                product_card_product: product, 
                lazy_load: true,
                show_vendor: true -%}
          </div>
        {%- else -%}
          <div class="collection-loading">
            <div class="collection-loading-spinner"></div>
            <div class="collection-loading-text">Loading amazing products...</div>
          </div>
        {%- endfor -%}
      </div>
      
      <!-- Pagination -->
      {%- if paginate.pages > 1 -%}
        <div class="pagination-wrapper">
          {%- render 'pagination', paginate: paginate -%}
        </div>
      {%- endif -%}
    </div>
  </div>

  <!-- Urgency Banner -->
  <div class="urgency-banner" id="urgencyBanner">
    <button class="urgency-banner-close" onclick="closeUrgencyBanner()">×</button>
    <div>⚡ Limited Time: Free shipping on orders over $75!</div>
  </div>
</section>

<script>
// 🎯 Ultra Collection JavaScript
document.addEventListener('DOMContentLoaded', function() {
  let currentView = 'grid';
  let currentSort = 'manual';
  
  // Sort products
  window.sortProducts = function(sortValue) {
    currentSort = sortValue;
    const productGrid = document.getElementById('productGrid');
    const products = Array.from(productGrid.querySelectorAll('.product-item'));
    
    products.sort((a, b) => {
      switch(sortValue) {
        case 'price-ascending':
          return parseFloat(a.dataset.price) - parseFloat(b.dataset.price);
        case 'price-descending':
          return parseFloat(b.dataset.price) - parseFloat(a.dataset.price);
        case 'title-ascending':
          return a.querySelector('.card-title').textContent.localeCompare(b.querySelector('.card-title').textContent);
        case 'title-descending':
          return b.querySelector('.card-title').textContent.localeCompare(a.querySelector('.card-title').textContent);
        case 'best-selling':
          return Math.random() - 0.5; // Random for demo
        default:
          return 0;
      }
    });
    
    // Animate product reordering
    products.forEach((product, index) => {
      product.style.order = index;
      product.style.opacity = '0';
      product.style.transform = 'translateY(20px)';
      
      setTimeout(() => {
        product.style.opacity = '1';
        product.style.transform = 'translateY(0)';
      }, index * 50);
    });
    
    showNotification(`Products sorted by ${sortValue}`, 'success');
  };
  
  // Set grid view
  window.setGridView = function(viewType) {
    currentView = viewType;
    const productGrid = document.getElementById('productGrid');
    const viewButtons = document.querySelectorAll('.view-toggle-btn');
    
    // Update active button
    viewButtons.forEach(btn => btn.classList.remove('active'));
    event.target.closest('.view-toggle-btn').classList.add('active');
    
    // Update grid class
    productGrid.classList.remove('list-view');
    if (viewType === 'list') {
      productGrid.classList.add('list-view');
    }
    
    // Animate transition
    const products = productGrid.querySelectorAll('.product-item');
    products.forEach((product, index) => {
      product.style.opacity = '0';
      product.style.transform = 'scale(0.9)';
      
      setTimeout(() => {
        product.style.opacity = '1';
        product.style.transform = 'scale(1)';
      }, index * 30);
    });
  };
  
  // Close urgency banner
  window.closeUrgencyBanner = function() {
    const banner = document.getElementById('urgencyBanner');
    banner.style.opacity = '0';
    banner.style.transform = 'translateY(100px)';
    setTimeout(() => {
      banner.style.display = 'none';
    }, 300);
  };
  
  // Auto-hide urgency banner after 10 seconds
  setTimeout(() => {
    const banner = document.getElementById('urgencyBanner');
    if (banner) {
      banner.style.opacity = '0.8';
    }
  }, 10000);
  
  // Intersection Observer for scroll animations
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.style.animationDelay = Math.random() * 0.5 + 's';
        entry.target.classList.add('animate-fade-in');
      }
    });
  });
  
  document.querySelectorAll('.product-item').forEach(item => {
    observer.observe(item);
  });
  
  // Real-time social proof updates
  setInterval(() => {
    const socialProofCount = document.querySelector('.social-proof-count');
    if (socialProofCount) {
      let currentCount = parseInt(socialProofCount.textContent.replace(/,/g, ''));
      currentCount += Math.floor(Math.random() * 3) + 1;
      socialProofCount.textContent = currentCount.toLocaleString();
    }
  }, 30000); // Update every 30 seconds
  
  // Show notification function
  function showNotification(message, type) {
    const notification = document.createElement('div');
    notification.className = `crowe-notification crowe-notification--${type}`;
    notification.textContent = message;
    notification.style.cssText = `
      position: fixed;
      top: 20px;
      right: 20px;
      padding: 12px 24px;
      border-radius: 8px;
      color: white;
      font-weight: 600;
      z-index: 9999;
      opacity: 0;
      transform: translateY(-10px);
      transition: all 0.3s ease;
    `;
    
    if (type === 'success') {
      notification.style.background = 'linear-gradient(135deg, rgba(34, 197, 94, 0.9), rgba(21, 128, 61, 0.9))';
    } else if (type === 'error') {
      notification.style.background = 'linear-gradient(135deg, rgba(239, 68, 68, 0.9), rgba(185, 28, 28, 0.9))';
    } else {
      notification.style.background = 'linear-gradient(135deg, rgba(59, 130, 246, 0.9), rgba(37, 99, 235, 0.9))';
    }
    
    document.body.appendChild(notification);
    
    setTimeout(() => {
      notification.style.opacity = '1';
      notification.style.transform = 'translateY(0)';
    }, 10);
    
    setTimeout(() => {
      notification.style.opacity = '0';
      notification.style.transform = 'translateY(-10px)';
      setTimeout(() => {
        document.body.removeChild(notification);
      }, 300);
    }, 3000);
  }
});
</script>

{% schema %}
{
  "name": "Ultra Collection",
  "tag": "section",
  "class": "section",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "collection",
      "id": "collection",
      "label": "Collection"
    },
    {
      "type": "range",
      "id": "products_per_page",
      "min": 8,
      "max": 50,
      "step": 2,
      "label": "Products per page",
      "default": 24
    },
    {
      "type": "checkbox",
      "id": "show_collection_image",
      "label": "Show collection image",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_collection_description",
      "label": "Show collection description",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_social_proof",
      "label": "Show social proof banner",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_conversion_triggers",
      "label": "Show conversion triggers",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_performance_metrics",
      "label": "Show performance metrics",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_urgency_banner",
      "label": "Show urgency banner",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "enable_filters",
      "label": "Enable smart filters",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "enable_sort",
      "label": "Enable sorting",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "enable_view_toggle",
      "label": "Enable view toggle",
      "default": true
    },
    {
      "type": "header",
      "content": "🎨 Visual Settings"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background color",
      "default": "#F4F4F2"
    },
    {
      "type": "color",
      "id": "accent_color",
      "label": "Accent color",
      "default": "#C6A351"
    },
    {
      "type": "header",
      "content": "🔥 Conversion Settings"
    },
    {
      "type": "text",
      "id": "social_proof_message",
      "label": "Social proof message",
      "default": "people purchased from this collection in the last 30 days"
    },
    {
      "type": "text",
      "id": "urgency_message",
      "label": "Urgency banner message",
      "default": "⚡ Limited Time: Free shipping on orders over $75!"
    }
  ],
  "presets": [
    {
      "name": "Ultra Collection"
    }
  ]
}
{% endschema %}
